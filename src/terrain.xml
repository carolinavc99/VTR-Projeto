<?xml version="1.0" ?>
<project name="Terrain generation" width=512 height=512>
	<assets>
        <attributes>
            <!-- noise parameters -->
			<attribute type="COLOR" name="K" data="FLOAT" value=1 />
            <attribute type="COLOR" name="amp" data="FLOAT" value=1000 />
		</attributes>
		<scenes>
			<scene name="MainScene">
                <geometry name = "Grid" type = "GRID" LENGTH=5000  DIVISIONS=1000>
                </geometry>
			</scene>
		</scenes>
		
		<viewports>
			<viewport name="MainViewport">
				<CLEAR_COLOR r="1.0" g="1.0" b="1.0" />
			</viewport>
		</viewports>
		
		<cameras>
			<camera name="MainCamera">
				<viewport name="MainViewport" />
				<POSITION x=0 y=500 z=0 />
				<VIEW x=0.0 y=-0.5 z=-1.0 />
				<UP x=0 y=1 z=0 />
			</camera>
		</cameras>
		
		<lights>
			<light name="Sun" >
				<DIRECTION x="1.0" y="-0.5" z="-2" />
			</light>
		</lights>
		
		<materialLibs>
			<materialLib filename="terrain.mlib"/>
		</materialLibs>
		
	</assets>
	
	
	<pipelines>
	
        <pipeline name="terrain" defaultCamera = "MainCamera">
		
			<pass class="default" name="create terrain" >
				<scenes>
					<scene name="MainScene" />
				</scenes>
				<camera name="MainCamera" />
				<viewport name="MainViewport" />
				<lights>
					<light name="Sun" />
				</lights>
				<materialMaps>
					<map fromMaterial="*" 
                                toMaterial="terrain"  toLibrary="Terrain" />
				</materialMaps>
			</pass>
		</pipeline>

    </pipelines>

	
	<interface>
		<window label="settings" >
			<pipelineList label="effect" />

            <var label="K" type="COLOR" context="Terrain::terrain" 
					component="K" def="min=0 max=3 step=0.1" />
            <var label="amp" type="COLOR" context="Terrain::terrain" 
					component="amp" def="min=600 max=1100 step=2" />
		</window>
	</interface>	
		

</project>